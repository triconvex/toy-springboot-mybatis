<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.zingoworks.springbootmybatis.board.dao.BoardMapper">

    <insert id="create" parameterType="io.zingoworks.springbootmybatis.board.model.Board">
        INSERT INTO board
        (title,
         content)
        VALUES (#{title},
                #{content})
    </insert>

    <select id="findById" parameterType="java.lang.Long" resultType="io.zingoworks.springbootmybatis.board.model.Board">
        SELECT *
        FROM board
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="io.zingoworks.springbootmybatis.board.model.Board">
        UPDATE
            board
        SET title   = #{title},
            content = #{content}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE
        FROM board
        WHERE id = #{id}
    </delete>

</mapper>